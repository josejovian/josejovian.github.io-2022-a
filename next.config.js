/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
	  domains: ['media-exp1.licdn.com', 'student-activity.binus.ac.id']
  }
}

module.exports = nextConfig
